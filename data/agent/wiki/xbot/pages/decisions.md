# Decisions

Durable design decisions for xbot.

- The first wiki phase uses Markdown files, directory indexes, file-system search, and LLM-maintained pages.
- RAG/vector search is a later derived enhancement, not a required dependency and not the source of truth.
